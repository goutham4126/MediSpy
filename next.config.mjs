/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'res.cloudinary.com',
            'img.clerk.com',
            'images.unsplash.com',
            
        ],

      },
};

export default nextConfig;
